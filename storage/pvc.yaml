kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pg-pvc
  labels:
    app: storage
spec:
  storageClassName: "local-storage"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi